#include "main.h"

/**
 * main - Entry point
 * Descreption: checks if the character is capital or not
 * Return: always 0 (Success)
 */

int main(void)
{

	int _isupper(int c)
	{
	c = 'A';
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	}
}
