#include "main.h"

/**
 * print_numbers - check the code
 * Descreption: a function that prints the numbers
 * Return: Always 0.
 */

void print_numbers(void)
{
		char i;

		for (i = 0; i < 10; i++)
		{
			_putchar(i + 48);
		}
		_putchar('\n');
}

