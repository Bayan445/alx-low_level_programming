#include "main.h"

/**
 * main - the first function
 * print_numbers - check the code
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
		char i;

		for (i = 0; i < 10; i++)
		{
			_putchar(i + 48);
			_putchar('\n');
		}
}
int main(void)
{
	return (0);
}
