#include "main.h"
#include <string.h>


/**
 * _strlen - a function tha tell the size
 * @s:a pointer name.
 * Return: len
 */

int _strlen(char *s)
{
	int len = 0;

	len = strlen(s);
	return (len);
}

