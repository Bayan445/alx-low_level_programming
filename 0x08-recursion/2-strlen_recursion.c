#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - a func that returns length
 *
 * @s: a string we will get it's length
 *
 * Return: The length of the string
 */
int _strlen_recursion(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
