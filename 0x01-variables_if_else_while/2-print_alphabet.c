#include <stdio.h>

/**
 * main - the entry point for the program
 *
 * Return: always 0.
 */

int main(void)
{
	char pe;

	for (pe = 'a'; pe <= 'z'; pe++)
	{
		putchar (pe);
	}
	putchar ('\n');

	return (0);
}
